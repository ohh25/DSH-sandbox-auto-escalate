{
  "name": "sandbox-auto-escalate",
  "version": "1.1.0",
  "private": true,
  "description": "DSH plugin: when a tool call is blocked by the sandbox, automatically re-dispatch it with wider permissions so the existing approval UI asks the user once — no wasted model round-trip. Ships a settings page for choosing which tool kinds and failure modes escalate.",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./client": "./client.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "client": {
      "platform": "web"
    }
  },
  "license": "MIT",
  "keywords": [
    "deepseek-harness",
    "dsh",
    "cordis",
    "plugin",
    "sandbox",
    "escalation",
    "approval"
  ]
}
